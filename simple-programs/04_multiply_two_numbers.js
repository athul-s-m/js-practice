function multiplication(multiplier, multiplicand) {
  return multiplier * multiplicand;
}

function main() {
  const multiplier = 10;
  const multiplicand = 20;

  const product = multiplication(multiplier, multiplicand);
  
  console.log("Product: " + product);
}

main();
