function sum(firstNumber, secondNumber) {
  return firstNumber + secondNumber;
}

function main() {
  const firstNumber = 10;
  const secondNumbe = 20;

  const result = sum(firstNumber, secondNumbe);
  
  console.log("Sum: " + result);
}

main();
